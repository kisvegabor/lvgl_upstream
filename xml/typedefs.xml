<typedefs>
    <typedef name="int"            help="normal number in the range of -2M..2M"/>
    <typedef name="px"             help="Simple pixel unit"/>
    <typedef name="%"              help="Percentage"/>
    <typedef name="content"        help="Can have a single value: LV_SIZE_CONTENT"/>
    <typedef name="subject"        help="A subject"/>
    <typedef name="string"         help="Simple `\0` terminated string" />
    <typedef name="enum"           help="Options from which one can be selected" />
    <typedef name="color"          help="A color stored as 24 bit RGB" />
    <typedef name="opa"            help="Opacity value in the range of 0..255 or 0..100%" />
    <typedef name="grad"           help="A complex gradient descriptor"/>
    <typedef name="obj"            help="Pointer to a widget" />
    <typedef name="animation"      help="An animation descriptor"/>
    <typedef name="font"           help="A font from the asset manager"/>
    <typedef name="image_src"      help="An image from the asset manager"/>
    <typedef name="style"          help="The name of a style">
    <typedef name="style+selector" help="The name of a style and state and part selector">
    <typedef name="state"          options="default|pressed|checked"       help="Style states, can be written after : in style names"/>
    <typedef name="part"           options="main|indicator|scrollbar"      help="Style parts, can be written after : in style names"/>
</typedefs>